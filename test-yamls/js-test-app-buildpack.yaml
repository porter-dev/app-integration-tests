version: v2
services:
- name: web
  run: node index.js
  type: web
  port: 3000
predeploy:
  run: ls
build:
  context: ./js-test-app
  method: pack
  builder: heroku/buildpacks:20
  buildpacks:
    - heroku/nodejs
